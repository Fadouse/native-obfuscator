// Autogenerated dispatch tables
static const std::array<void*, OP_COUNT> label_table = {
    &&do_push, &&do_junk2, &&do_div, &&do_add, &&do_mul, &&do_junk1, &&do_swap, &&do_sub, &&do_print, &&do_dup, &&junk, &&halt,
};
static const std::array<uint8_t, OP_COUNT> label_index = {
    0, 3, 7, 4, 2, 8, 11, 10, 5, 1, 6, 9,
};
